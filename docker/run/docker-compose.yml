services:
  agent-zero:
    image: agent0ai/agent-zero:latest
    user: "0:0"                        # root inside container
    security_opt:
      - no-new-privileges:false        # allow setuid here only
    cap_drop:
      - ALL                            # still no kernel caps
    ports:
      - "50080:80"
